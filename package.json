{
  "name": "@crypto/root",
  "private": true,
  "scripts": {
    "start": "lerna run --scope @crypto/* --parallel start",
    "build": "lerna run --scope @crypto/* --parallel build",
    "serve": "lerna run --scope @crypto/* --parallel serve",
    "clean": "lerna run --scope @crypto/* --parallel clean",
    "lint": "lerna run --scope @crypto/* lint",
    "bootstrap": "lerna bootstrap --ignore-scripts"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
